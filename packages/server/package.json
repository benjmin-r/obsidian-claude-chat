{
	"name": "@occ/server",
	"version": "0.1.0",
	"description": "Claude Agent SDK service: server-owned long-lived sessions relayed to the Obsidian plugin over a token-authenticated WebSocket.",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"start": "node dist/index.js",
		"test": "jest",
		"test:coverage": "jest --coverage"
	},
	"dependencies": {
		"@anthropic-ai/claude-agent-sdk": "0.3.179",
		"@occ/protocol": "0.1.0",
		"ws": "^8.18.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"@types/ws": "^8.5.10"
	},
	"license": "MIT"
}
