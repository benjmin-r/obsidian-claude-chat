{
	"name": "@occ/plugin",
	"version": "0.1.0",
	"description": "Obsidian plugin: a structured Claude chat sidebar backed by the Agent SDK server over Tailscale.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "jest",
		"test:coverage": "jest --coverage"
	},
	"dependencies": {
		"@occ/protocol": "0.1.0"
	},
	"devDependencies": {
		"@types/jest": "^29.5.0",
		"@types/node": "^20.0.0",
		"esbuild": "0.25.5",
		"jest": "^29.5.0",
		"jest-environment-jsdom": "^30.2.0",
		"obsidian": "latest",
		"ts-jest": "^29.1.0",
		"typescript": "^5.8.3"
	},
	"license": "MIT"
}
